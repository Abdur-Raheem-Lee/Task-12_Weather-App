import React from 'react';

const Title = (props) =>{
    return (
        <div>
            <h1 className="titel-container_title">Weather App </h1>
            <p className="title-container_subtitle">Enter the city of a any country to see what the weather is...</p>
        </div>
    )
}

export default Title;